# Auto-fix Instructions for KnowYea v1.3.161 Prep

## Issue 1: profile.wxml - model:value for custom interest input
- **File**: `miniprogram/pages/profile/profile.wxml`, line 81
- **Fix**: Change `value="{{_customInputValue}}"` to `model:value="{{_customInputValue}}"` for two-way binding

## Issue 2: profile.wxml - Remove dead settings link
- **File**: `miniprogram/pages/profile/profile.wxml`, line 135
- **Detail**: Settings page was deleted, this link navigates to a non-existent page
- **Fix**: Remove the entire settings menu-item block (lines 135-139), or redirect to garden

## Issue 3: profile.js - Remove dead settings route
- **File**: `miniprogram/pages/profile/profile.js`, line 420
- **Fix**: Remove `settings: '/pages/settings/settings'` from the routes object

## Issue 4: garden.js - Remove dead settings route
- **File**: `miniprogram/pages/garden/garden.js`, line 180
- **Detail**: garden.wxml already removed the settings menu-item; the route mapping is now dead code
- **Fix**: Remove `settings: '/pages/settings/settings'` from the routes object in onNavigateTo

## Issue 5: history.wxml - Fix data attributes to match getHistory enriched response
- **File**: `miniprogram/pages/history/history.wxml`, line ~25
- **Detail**: The template uses `data-theme-id="{{record.themeId}}"` and `data-node-id="{{record.nodeId}}"`, but getHistory returns `courseId` and `lessonId` from the progress collection. The JS handler (history.js line 108-110) already handles the fallback correctly with `const targetThemeId = courseId || themeId`, but the template should use the correct field names to avoid relying on fallback logic.
- **Fix**: Change `data-theme-id="{{record.themeId}}"` to `data-course-id="{{record.courseId}}"` and `data-node-id="{{record.nodeId}}"` to `data-lesson-id="{{record.lessonId}}"`
