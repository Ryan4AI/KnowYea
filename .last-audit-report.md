# 小知也项目审计报告 #2026-05-19/3

## 概述

审计时间：2026-05-19 12:02（午间定时审计）
Git 状态：✅ 本地提交完成（+2 个新 commit），❌ 推送失败（HTTPS 443 超时，SSH 无密钥）
最新本地提交：`9759fbcd` - fix: add missing fail handler for loadLearningCourses cloud call

## ✅ 本轮自动修复的 A 类问题

### 1. theme-store.wxml 头部冗余清理（遗留改动）

| 文件 | 改动 |
|------|------|
| `miniprogram/pages/theme-store/theme-store.wxml` | 将 🧠 图标改为 ✨，移除冗余的 `page-title`（导航栏已显示 "AI 课程生成"） |

**提交：** `4d629b3f` — v1.3.147: finish header cleanup
**说明：** v1.3.146 改了导航栏标题和生成弹窗图标，但页面头部 icon 和 title 未同步更新。此改动是 v1.3.146 的自然延续，完成后页面头部仅留下 `page-icon` + `page-desc`，简洁不重复。

### 2. 🔴 三个云函数的硬编码 API Key 泄露

| 云函数 | 之前 | 当前 |
|--------|------|------|
| `sendMessage/index.js` | 🔴 含 fallback | ✅ 仅 `process.env.MINIMAX_API_KEY` |
| `completeLesson/index.js` | 🔴 含 fallback | ✅ 仅 `process.env.MINIMAX_API_KEY` |
| `createCourse/index.js` | 🔴 含 fallback | ✅ 仅 `process.env.MINIMAX_API_KEY` |

**提交：** `cdca57e0` — security: remove hardcoded MiniMax API key
**说明：** 之前的 fix commit `2a6ae275` 在配置环境变量时"顺便"恢复了 fallback，导致敏感 key 重入 git。本次摘除全部 fallback，仅保留环境变量读取。如需本地开发测试，请配置 `.env` 或终端 `export`。

### 3. loadLearningCourses 缺少 fail 处理

| 文件 | 位置 | 之前 | 当前 |
|------|------|------|------|
| `learn.js` | L198 | `success` 无 `fail` | ✅ 添加 `fail: () => console.warn(...)` |

**提交：** `9759fbcd` — fix: add missing fail handler for loadLearningCourses cloud call

## 代码质量抽样检查

### learn.js（学习核心页面）
| 检查项 | 状态 |
|--------|------|
| cloud.callFunction 调用的 success/fail 覆盖 | ✅ 8/8 全部覆盖（本轮补齐 1 处遗漏） |
| sendMessage 调用完整（params/超时） | ✅ |
| AI 回复剥离 `<think>` 标签 | ✅ |
| parseMessageBlocks JSON 解析 | ✅ |
| 加载/空/错误状态 | ✅ 有 splash/welcome/loading/完成状态 |
| 选项交互（选择题/问答题） | ✅ |
| 进度推进（下一节/完成课程） | ✅ |

### garden.wxml（首页）
| 检查项 | 状态 |
|--------|------|
| 加载状态 | ✅ `.loading` 显示 🌱 |
| 空状态（无课程） | ✅ "你还没有课程" + 引导 |
| 连击统计 | ✅ `.streak-bar` |
| 学习时间线 | ✅ `.timeline-cell` + active/empty 类 |
| 最近记录和成就 | ✅ 带 badge 计数 |

### theme-store.wxml（课程生成页）
| 检查项 | 状态 |
|--------|------|
| 加载状态 | ✅ `!profileLoaded` 显示 |
| 未设置画像引导 | ✅ 完整卡片 |
| 输入/生成流程 | ✅ 含进度条弹窗 |
| 生成完成提示 | ✅ 新课程确认弹窗 |
| 兴趣标签加载 | ✅ 动态展示 |

### 颜色一致性
| 文件 | 状态 |
|------|------|
| 所有 .wxss 中的 `#fff` 使用 | ✅ 均在有色背景上（导航/标签选中/用户气泡），非 `#fff` 背景 |
| `pages.wxss` L43 `#fff` | ✅ 导航栏深色背景 |
| `pages.wxss` L90 `#fff` | ✅ accent 色按钮文字 |
| 使用 CSS 变量 | ✅ `--capsule-accent`, `--capsule-bg`, `--capsule-surface`, `--capsule-ink` |

## B 类问题（需决策，无变化）

| # | 问题 | 优先级 | 说明 |
|---|------|--------|------|
| 1 | helpers.js mdToHtml 硬编码颜色 | 低 | 等 dark mode 时一并处理 |
| 2 | 概念卡片 4 种配色无 CSS 变量 | 低 | 现有设计合理，可等下一步迭代 |
| 3 | 无 dark mode 支持 | 低 | 需 UX 设计与决策 |
| 4 | 竞品「高考AI学长」在微信生态定位重合 | 中 | 参见 insights/latest.md 详细分析 |

## 🧠 行业追踪提醒

行业动态追踪 cron 上次运行于 2026-05-18，按计划应在 2026-05-21（周四 09:00）再次扫描。当前竞品分析主要关注点为：
- 腾讯 AI 智能体开放能力（潜在流量红利）
- 混元 Hy3 preview API 升级时机（Q3 前评估）
- 豆包付费分层模式参考

## 未推送的 Commits（7 个）

由于网络不可达（HTTPS 443 超时、SSH 无密钥），以下 commit 仅在本地：

```
434a3590 v1.3.146: rename 课程库→AI 课程生成; replace 🧠 icons with ✨⚡
4e7c9fbd v1.3.145: fix theme-store tags loading from user_tags
007f44a0 fix: loadInterestTags读user_tags集合而非已废弃的users.interests
2a6ae275 fix: 给3个云函数配环境变量MINIMAX_API_KEY + 恢复fallback
ddf60e6d security: remove re-introduced hardcoded MiniMax API key
cdca57e0 security: remove hardcoded MiniMax API key from all 3 cloud functions
4d629b3f v1.3.147: finish header cleanup — remove redundant page-title
9759fbcd fix: add missing fail handler for loadLearningCourses cloud call
```

需在恢复网络后（或配置 SSH key 后）执行 `git push origin main`。

## 下次审计关注点

1. ✅ 本轮已补齐所有 cloud.callFunction 的 fail 处理
2. 继续关注新增云函数是否含硬编码 API Key
3. 持续跟进 git push 失败，7 个本地 commit 待推送
4. 注意 helpers.js 中的硬编码颜色，等 dark mode 决策
