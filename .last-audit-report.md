# 小知也项目审计报告 #2026-05-19/1

## 概述

审计时间：2026-05-19 00:02（凌晨定时审计）
Git 状态：已提交并推送（3 files changed, +3/-3）
项目路径：/home/admin/workspace/knowyea
最后提交：`d3299c5c` - security fix + garden colors

## 本次发现的 A 类问题（自动修复）

### 1. 🔴 安全漏洞：2 处硬编码 MiniMax API Key

| 文件 | 行号 | 问题 |
|------|------|------|
| `cloudfunctions/completeLesson/index.js` | L7 | `const API_KEY = process.env.MINIMAX_API_KEY \|\| 'sk-cp-...DgCDM'` |
| `cloudfunctions/createCourse/index.js` | L7 | 同上 |

**风险**：云函数代码上传到微信云开发后，源代码中包含 800+ 字符的完整 MiniMax API Key，任何有环境读取权限的人都能窃取。

**修复**：移除 `|| 'sk-...'` fallback，仅保留 `process.env.MINIMAX_API_KEY`（与 sendMessage 一致）。

**验证**：sendMessage 云函数已在之前审计中修复，现仅通过环境变量读取 key，工作正常。

### 2. 🎨 花园页残留硬编码颜色

| 文件 | 行号 | 硬编码 | 替换后 |
|------|------|--------|--------|
| `garden.wxss` | L135 | `color: #fff` | `var(--capsule-surface)` |
| `garden.wxss` | L149 | `background: #f0f0f0` | `var(--capsule-bg)` |

### 3. 其他文件颜色状态确认

上一轮审计列出 14 个文件需要修复，本轮复查结果：

- ✅ **已修复**（上一轮提交 `7fb57d99` 已覆盖）：`learn.wxss` 大部分变量化、`pages.wxss`、`gen-loading.wxss`、组件颜色
- ✅ **无需修复**（#fff 文字在彩色背景上是正确用法）：`learn.wxss` 中的 `#fff`、`theme-store.wxss` 中的 `#fff`、`component` 中的 `#fff`
- ✅ **本次已修复**：`garden.wxss` 剩余 2 处

## 代码质量检查

### learn.js — 核心学习页面（477行）
- ✅ `sendMessage` 组装 system prompt 完整（课程、课时、用户画像、学习档案）
- ✅ `parseMessageBlocks` 正确解析概念/例子/总结/评价标签
- ✅ AI 回复去 `<think>` 标签逻辑完整
- ✅ 空课程→跳商店、无画像→跳表单 的用户流转通畅
- ✅ 快捷键 `onOptionTap` 和 `onOpenSubmit` 绑定正确
- ⚠️ 注意：加载课程时 `loadPageData` 中 `courseContext` 来自 `currentCourse.lessonSummary`，若 summary 为空则回退到"暂无历史学习记录"。建议未来考虑从历史消息摘要生成更精确的上下文

### sendMessage/index.js — 云函数（181行）
- ✅ API Key 仅有 `process.env.MINIMAX_API_KEY`
- ✅ `parseCompletion` 已含 `<think>` 标签剥离
- ✅ JSON action/score 解析正确
- ✅ 历史消息限制 20 条
- ✅ 返回格式包含 `aiReply`, `isCompleted`, `score`

### helpers.js — 工具函数
- ✅ `parseMessageBlocks` 正确识别 5 种标签 + choice/open 题目
- ⚡ `mdToHtml` 中有少量硬编码颜色（`#333`, `#f5f5f7`, `#f0f0f2`, `#e74c3c` 等），但这些是 rich-text 页内样式，不影响主题一致性，B 类问题

## B 类问题（需决策）

| # | 问题 | 建议 |
|---|------|------|
| 1 | helpers.js mdToHtml 中硬编码颜色 | 低优先级，这些是 rich-text 渲染样式，只在 rich-text 组件内生效，可等统一 dark mode 支持时一并处理 |
| 2 | 学习页概念卡片有 4 种颜色方案（紫/蓝/绿/橙） | 合理设计，但 `#f4f4ff`, `#f0f9ff`, `#0ea5e9` 等没有对应 CSS 变量。未来可考虑定义 `--card-concept-bg`, `--card-example-bg` 等变量 |
| 3 | 无 dark mode 支持 | app.wxss 中没有 `prefers-color-scheme: dark` 媒体查询。目前所有 `#fff` text-on-colored-bg 模式在暗色模式下可能出问题 |

## 自动修复执行结果

```
✓ cloudfunctions/completeLesson/index.js — 移除硬编码 API key
✓ cloudfunctions/createCourse/index.js — 移除硬编码 API key
✓ miniprogram/pages/garden/garden.wxss — #fff → var(--capsule-surface), #f0f0f0 → var(--capsule-bg)
✓ 已提交 & 推送至 GitHub
```

## 下次审计关注点

1. 如果混元 API 替换 MiniMax，注意所有 4 个使用 API_KEY 的云函数
2. 添加新页面时注意 CSS 变量一致性
3. 确保任何新的 cloud call 都有完整 success/fail 处理
