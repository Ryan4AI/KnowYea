# 小知也项目审计报告 #2026-05-19/2

## 概述

审计时间：2026-05-19 08:02（早间定时审计）
Git 状态：✅ 已提交（1 个文件修复），❌ 推送失败（网络不通）
最后提交：`ddf60e6d` - security: remove re-introduced hardcoded MiniMax API key in sendMessage

## 🔴 本轮发现的 A 类问题（自动修复，已执行）

### 1. sendMessage 云函数硬编码 API Key（安全漏洞）

| 文件 | 状态 | 详情 |
|------|------|------|
| `cloudfunctions/sendMessage/index.js` L7 | 🔴 → ✅ **已修复** | 上轮审计 `d3299c5c` 已移除，但随后的 `64f82180`（v1.3.143 超时修复 30s→60s）不小心基于旧版本重引入。本次已重新移除 fallback，保留 60s 超时。 |

### 2. 安全姿态确认 — 所有云函数

| 云函数 | 之前状态 | 当前状态 |
|--------|---------|---------|
| `sendMessage` | 🔴 含 fallback | ✅ 仅 `process.env.MINIMAX_API_KEY` |
| `completeLesson` | ✅ 已在上一轮修复 | ✅ 干净 |
| `createCourse` | ✅ 已在上一轮修复 | ✅ 干净 |
| `generateTheme` | N/A（此云函数不存在，使用 `createCourse` 替代） | — |

### 3. Garden 颜色确认

| 文件 | 之前状态 | 当前状态 |
|------|---------|---------|
| `garden.wxss` L148 progress-bar | 已由 `ff3cfe97` 修复 | ✅ `var(--capsule-bg)` / `var(--capsule-accent)` |
| `garden.wxss` L135 `#fff` | 已由 `d3299c5c` 修复 | ✅ `var(--capsule-surface)` |
| `garden.wxss` L149 `#f0f0f0` | 已由 `d3299c5c` 修复 | ✅ `var(--capsule-bg)` |

## 代码质量检查（抽样）

### learn.js — 核心学习页面
- ✅ `sendMessage` 调用正常，params 完整
- ✅ `loadPageData` 中 `courseContext` 逻辑正常
- ✅ AI 回复剥离 `<think>` 标签正确
- ✅ 所有 cloud call 有 success/fail 处理

### sendMessage/index.js — 云函数（181行）
- ✅ API Key 安全（仅环境变量）
- ✅ `parseCompletion` JSON 解析先剥 `<think>` 正常
- ✅ 历史消息限制 20 条
- ✅ HTTP 超时 60s

### colors.wxss 审核（白底有色背景上的 #fff）
- `learn.wxss` 中的 `#fff` — ✅ 正确（用户消息气泡在有色背景上）
- `profile.wxss` 中的 `#fff` — ✅ 正确
- `theme-store.wxss` 中的 `#fff` — ✅ 正确

## B 类问题（无变化，沿用上轮）

| # | 问题 | 优先级 | 状态 |
|---|------|--------|------|
| 1 | helpers.js mdToHtml 硬编码颜色 | 低 | 等候 dark mode 支持时一并处理 |
| 2 | 概念卡片 4 种配色无 CSS 变量 | 低 | 合理设计，需决策 |
| 3 | 无 dark mode 支持 | 低 | 需 UX 设计与决策 |

## Git 操作

```
commit ddf60e6d
Author: 久月 <agent>
Date:   Tue May 19 08:02:00 2026 +0800

    security: remove re-introduced hardcoded MiniMax API key in sendMessage
    (was re-added by v1.3.143 timeout fix)
```

⚠️ **推送失败**：服务器无法连接到 GitHub（网络超时）。commit 已本地保存，恢复网络后将自动推送或由下次审计时处理。

## 下次审计关注点

1. ✅ 本轮修复后所有 4 个云函数 API Key 均无硬编码 fallback
2. 如新增云函数，注意 MiniMax API Key 必须仅从环境变量读取
3. 混元 API 迁移时需同步修改所有云函数的 API endpoint
4. 注意 git push 失败的状态，下次尝试推送未推送的 commits
