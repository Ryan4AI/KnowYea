# 自动审计报告 — 2026-05-19 20:02

## 审计范围
- 项目文件：PROJECT.md, README.md, insights/
- 代码审计：cloudfunctions (16 个), pages (learn/garden/profile/history/theme-store), utils/helpers.js, app.wxss
- 检查项：颜色一致性、API Key 泄露、事件绑定缺失、CSS 死代码、错误处理完整性

## 状态总结

### ✅ 安全检查
- [pass] 无硬编码 API key（所有 3 个使用 MiniMax 的云函数均通过 `process.env.MINIMAX_API_KEY` 获取）
- [pass] 无 `sk-` 等密钥模式出现在源代码中
- [pass] 用户 openid 通过云函数传递，前端不直接存储敏感信息
- [pass] 所有云函数有 try/catch 错误处理

### ✅ 颜色一致性
- [pass] 主色调 `--capsule-accent: #6c5ce7` 统一使用 CSS 变量引用
- [pass] 概念卡片颜色（概念 #6c5ce7、例子 #0ea5e9、总结 #22c55e、评价 #f59e0b）为分类标记色，属于有意设计
- [pass] garden 页的 `--capsule-accent-2: #a29bfe` 和渐变引用一致
- [pass] 无其他硬编码色值出现在页面级 wxss 中

### ✅ 事件绑定完整性
- [pass] learn.wxml: `onOptionTap`, `onOpenSubmit`, `doSend`, `manualAdvance`, `onFinishCourse`, `onGardenTap`, `onInputChange` 均已实现
- [pass] garden.wxml: `onCourseTap`, `onNavigateTo`, `onAddTheme`, `confirmDeleteTheme`, `onNewTheme`, `onEditProfile` 均已实现
- [pass] 按钮禁用状态使用了 `disabled` 属性绑定

### ✅ 错误处理
- [pass] 所有云函数成功/失败回调均已实现（含最近两轮修复：manualAdvance 和 loadLearningCourses）
- [pass] sendMessage 云函数对 `miniMaxMessages` 新路径和传统路径都正确处理
- [pass] completeLesson 云函数含成就解锁、摘要生成、history 记录三层逻辑

### ✅ 代码组织
- [pass] 无阻塞死代码残留
- [pass] 35 天内无新增大文件（仅维护性改动）
- [pass] 所有 console.error 用于正确诊断场景，非 debug 残留

### ⚠️ 建议项（B 类，需决策）

1. **getHistory limit 硬编码 50**  
   `cloudfunctions/getHistory/index.js:17` — limit 固定为 50，但 garden 页实际只需 3 条就调了 getHistory。需决策：是否让 getHistory 也接收 limit 参数？目前 garden 额外多拉了 47 条无用数据。

2. **历史版本 v1.3.x 标记残留**  
   `PROJECT.md` 仍标记当前版本为 v1.3.147。需决策是否 bump 版本号。

3. **pendingTransition 状态**  
   learn.js 有 `isPendingTransition` 字段但仅有初始化设置，没有实际的 transition 逻辑利用它。需确认是否为预留接口。

### 自动修复统计
无 — 代码库状态干净，上一轮所有 A 类问题已于 16:02 修复并提交。

## Git 状态
- 最新提交：`0962f8b1` — fix: add missing fail handler for manualAdvance completeLesson cloud call
- 未提交改动：仅本审计报告文件
- 远程：已同步（上次 push 随 commit 完成）
