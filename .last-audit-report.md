# 小知也项目审计报告 #2026-05-18/2

## 概述

审计时间：2026-05-18 20:02（上次审计：4小时前）
Git 状态：干净（上次审计已提交）
项目状态：PROGRESS.md 已更新至第3阶段，描述为"学习功能稳定，多课程支持，AI对话"

## A类问题（自动修复）

### 1. learn.wxss — 大量硬编码颜色（最高优先）
主学习页面仍有约18处硬编码颜色值，需替换为CSS变量：

| 硬编码 | 替换变量 |
|--------|----------|
| `#fff` (背景) | `var(--capsule-surface)` |
| `#eee` (边框) | `var(--capsule-border)` |
| `#333` (主文本) | `var(--capsule-ink)` |
| `#999` (辅助文本) | `var(--capsule-muted)` |
| `#bbb` (时间戳) | `var(--capsule-muted)` |
| `#d1d5db` (禁用按钮) | `var(--capsule-border)` |
| `#e5e7eb` (输入框边框) | `var(--capsule-border)` |
| `#374151` (选项文本) | `var(--capsule-ink-secondary)` |
| `#22c55e` | `var(--capsule-success)` |
| `#f59e0b` | `var(--capsule-warm)` |
| `#7c3aed` (题目标签) | `var(--capsule-accent)` |
| `#f97316` (评分橙) | `var(--capsule-warm)` |
| `#f5f3ff` | 可保持或简化 |
| `#e8d5ff` (题目边框) | `rgba(108, 92, 231, 0.15)` |

### 2. styles/pages.wxss — 共享样式硬编码
- `#fff` → `var(--capsule-surface)`
- `#999`, `#666`, `#333`, `#555`, `#aaa` → 对应变量
- `#d1d5db` → `var(--capsule-border)`

### 3. styles/gen-loading.wxss — 生成进度条硬编码
- `#fff` → `var(--capsule-surface)`
- `#999` → `var(--capsule-muted)`
- `#e8e8f0` → `rgba(26, 29, 46, 0.08)` 或保持

### 4. garden.wxss — 遗留硬编码（上次已部分修）
- `#e74c3c` (删除按钮) → `#d54941` (错误色) 或自定义变量
- `#f0fdf4` (完成标签) → `rgba(0, 201, 167, 0.1)`
- `#22c55e` → `var(--capsule-success)`
- `#bbb`, `#ccc` → `var(--capsule-muted)`
- `#fff7ed` (连续学习条) → 保持或使用 `rgba(253, 203, 110, 0.1)`
- `#c2410c` (streak文字) → `var(--capsule-warm)` 加深版

### 5. 其他页面
- **favorites.wxss**: #333, #999 → 变量
- **history.wxss**: #999, #bbb, #fff → 变量  
- **profile.wxss**: #fff, #666, #eee → 变量
- **settings.wxss**: #333, #ccc → 变量
- **theme-store.wxss**: #999, #fff, #d1d5db, #bbb, #666 → 变量
- **theme-store.wxml**: 4处 inline style 硬编码 → 提取至wxss类

### 6. 组件 — 硬编码颜色
- **message-card.wxss**: #4CAF50 → var(--capsule-accent); #fff, #333, #999, #e8f5e9, #e3f2fd → 变量
- **question-card.wxss**: #6c5ce7 → var(--capsule-accent); #fff, #fafbff, #eef0f6 → 变量
- **theme-switcher.wxss**: 大量硬编码 → 变量替换
- **achievement-popup.wxss**: dark主题有硬编码颜色 → 部分（#fff, #6c5ce7）可替换

### 7. sendMessage/index.js — 安全漏洞
第20行：`const API_KEY = process.env.MINIMAX_API_KEY || 'sk-...'` 
硬编码的 API key fallback 是安全风险，应立即移除。只保留 `process.env.MINIMAX_API_KEY`。

## B类问题（需决策）

无新的 B 类问题发现。上次报告中的"无课程时花园空状态指引"已在 PROGRESS.md 中被标记。

## 自动修复执行

以下文件将由 Dev Agent 自动修复（CSS变量替换 + 安全加固）：
1. `miniprogram/pages/learn/learn.wxss` — 主学习页颜色修正
2. `miniprogram/styles/pages.wxss` — 共享样式颜色修正
3. `miniprogram/styles/gen-loading.wxss` — 生成进度条颜色修正
4. `miniprogram/pages/garden/garden.wxss` — 花园页剩余颜色修正
5. `miniprogram/pages/favorites/favorites.wxss` — 收藏页颜色修正
6. `miniprogram/pages/history/history.wxss` — 历史页颜色修正
7. `miniprogram/pages/profile/profile.wxss` — 个人页颜色修正
8. `miniprogram/pages/settings/settings.wxss` — 设置页颜色修正
9. `miniprogram/pages/theme-store/theme-store.wxss` + `.wxml` — 主题商店颜色修正
10. `miniprogram/components/message-card/message-card.wxss` — 消息卡片颜色修正
11. `miniprogram/components/question-card/question-card.wxss` — 题目卡片颜色修正
12. `miniprogram/components/theme-switcher/theme-switcher.wxss` — 主题切换器颜色修正
13. `miniprogram/components/achievement-popup/achievement-popup.wxss` — 成就弹窗颜色修正
14. `cloudfunctions/sendMessage/index.js` — 移除硬编码API key
